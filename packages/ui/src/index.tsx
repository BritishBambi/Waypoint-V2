// Shared UI components
// Note: components here should use React Native primitives so they are
// compatible with both apps/web (via react-native-web) and apps/mobile.
// Alternatively, use platform-specific file extensions (.web.tsx / .native.tsx).

export { Button } from "./Button";
